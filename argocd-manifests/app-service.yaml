apiVersion: v1
kind: Service
metadata:
  name: cga-app-service
spec:
  selector:
    app: cga-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
      name: http
  type: NodePort
